dist
node_modules
.plan
package-lock.json
screenshots

# Generated by scripts/build-catalog.mjs, which picks its own indentation to
# keep the diffs readable. Letting Prettier reformat them means every
# `catalog:build` leaves the tree dirty in a way `format` then has to undo.
src/lib/catalog/data/*.json
